server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost/bootcamp2024
    username: ${username}
    password: ''

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    #properties:
      #hibernate:
        #dialect: org.hibernate.dialect.MySQLDialect